#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int var;

	for (var = 0; var < 10; var++)
	{
		printf("%d", var);
	}
	printf("\n");
	return (0);
}
